# Fully disable coredumps: no core files are ever written (Storage=none) and
# zero-size cores are refused too (ProcessSizeMax=0). Effective immediately on
# the next core dump event - no daemon restart needed (systemd-coredump(8)).
[Core]
Storage=none
ProcessSizeMax=0