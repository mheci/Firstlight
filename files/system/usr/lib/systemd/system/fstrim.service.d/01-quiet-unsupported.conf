# Bazzite's fstrim workaround: --quiet-unsupported silences fstrim on
# filesystems that don't support TRIM (e.g. some zram/btrfs configs).
[Service]
ExecStart=/usr/bin/fstrim --listed-in /proc/self/mountinfo --verbose --quiet-unsupported
